.venv/
__pycache__/
*.pyc
.env
data/

# reports/ is deliberately NOT ignored: reports/latency.* and reports/ablation.*
# are the evidence for every number in the README. A reader who cannot see the
# artifact has to take the table on trust, which is the whole thing this project
# is trying not to do.
node_modules/
.next/
.pytest_cache/

# Regenerated by web/scripts/copy-vad-assets.mjs on postinstall. ~40 MB of
# ONNX/WASM that npm already pins -- committing it would quadruple the clone.
web/public/

# TypeScript incremental build cache; regenerated by tsc.
web/tsconfig.tsbuildinfo
